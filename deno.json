{
	"lock": false,
	"fmt": {
		"useTabs": true
	},
	"tasks": {
		"add": "GITHUB_TOKEN=$(gh auth token) deno run --unstable-temporal -A add.ts",
		"search": "GITHUB_TOKEN=$(gh auth token) deno run --unstable-temporal -A search.ts",
		"search-anywidget": "GITHUB_TOKEN=$(gh auth token) deno run --unstable-temporal -A search.ts anywidget",
		"search-ipywidgets": "GITHUB_TOKEN=$(gh auth token) deno run --unstable-temporal -A search.ts ipywidgets"
	},
	"lint": {
		"rules": {
			"exclude": ["prefer-const"]
		}
	}
}
